# Basiq API (optional - for automatic bank sync)
# BASIQ_API_KEY=

# Bank credentials are not stored here.
# Use 'ledger download <source>' for browser-based statement downloads,
# or manually place statement files in staging/<source>/ and run 'ledger ingest'.
